CREATE TABLE IF NOT EXISTS `sgp`.`chale` (
	`id`	INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT UNIQUE,
	`nome`	TEXT NOT NULL,
	`camasCasal`	INTEGER NOT NULL DEFAULT 0,
	`camasSolteiro`	INTEGER NOT NULL DEFAULT 0,
	`descricao`	TEXT
);